import * as React from 'react';

/**
 * Palette props.
 */
export interface PaletteProps {
  /** Token names without the --d47- prefix. Defaults to the full base palette. */
  tokens?: string[];
}

export declare function Palette(props: PaletteProps): JSX.Element;

/** The raw class names, for hand-written markup. */
export declare const PaletteClasses: {

};
